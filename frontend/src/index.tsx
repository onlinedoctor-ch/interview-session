import React from "react";
import ReactDOM from "react-dom";
import { App } from "./components/app";

const applicationContainer = document.getElementById("applicationContainer");
ReactDOM.render(<App />, applicationContainer);
